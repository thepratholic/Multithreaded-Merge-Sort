#include <bits/stdc++.h>
#include <iostream>
#include "sorting/mergeSort.hpp"
using namespace std;

int main(int argc, char *argv[]) {

    const int SIZE = 1000;
    vector<int> nums(SIZE);

    for(int i = 0; i < SIZE; i++) {
        nums[i] = rand() % 1000;
    }

    
}